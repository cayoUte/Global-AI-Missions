# AI usage log — Global AI Missions

> Owner: `tech-lead`. Running log, newest at the bottom. Every agent appends an entry when it delivers; **the human fills in or corrects the "Human review" line** — an agent never claims a review that did not happen.
> Source for `docs/AI_USAGE.md` (Spanish, delivery-engineer), which must be truthful (brief §10: declare the AI tools used and what the candidate did personally).
> Tooling for every entry unless stated otherwise: Claude Code with the subagents defined in `.claude/agents/`, orchestrated by the human.

Entry template:

```md
### <n>. <YYYY-MM-DD> · <agent> — <task>
- Files: <created / changed>
- What the agent did: <1–3 lines>
- Verification: <commands run and results, or "not run" and why>
- Human review: <what the human read, decided, rewrote — or "pending">
```

---

### 1. 2026-09-24 · `product-architect` — product spec and catalog

- Files: `docs/product/PRODUCT.md`, `docs/product/REQUIREMENTS_MAP.md`, `docs/product/MAYA.md`, `docs/product/DECISIONS_LOG.md` (PD-001 … PD-030), `content/catalog.json`.
- What the agent did: turned the fixed concept into journeys, the core loop per screen, screen acceptance criteria with all states (§5), MVP scope and out-of-scope list, microcopy, Maya's character bible, the requirements map against the brief and the five-mission catalog with deterministic unlock rules.
- Verification: `content/catalog.json` validates against `docs/contracts/catalog.schema.json` (checked by the tech-lead at G0, see entry 3).
- Human review: decisions marked "Decided (human, 2026-09-24)" in DECISIONS_LOG were taken by the human (PD-008, PD-009, PD-011, PD-014, PD-018, PD-023, PD-025, PD-027; PD-001 delegated). _Human: add what you read and anything you rewrote._

### 2. 2026-09-24 · human-directed audit and spec revision

- Files: `docs/agents/SHARED_CONTEXT.md`, `docs/agents/README.md`, all twelve briefs in `.claude/agents/`, and the matching entries in `docs/product/` (DECISIONS_LOG, PRODUCT).
- What changed (directed by the human, edits made with an AI assistant — _human: confirm the tool and who wrote what_):
  - Mission Report order: **numbers first** (label `A2 · The Last Train — 70%`, global %, per skill, correct/incorrect, level with reason, attempt record), then Maya's interpretation, next mission, Diary, missed checkpoints (PD-001).
  - **No skip button**: every checkpoint needs a decision, so correct + incorrect always equals 10 and "unanswered = incorrect" disappeared (PD-011).
  - **8-hour budget** instead of 10 (brief §10): shorter timeboxes, a walking-skeleton gate G1 at 2:30 on a fixture mission, polish and `/simulate` moved to stretch, calibration as one informational run (PD-027).
  - Contract changes in SHARED_CONTEXT §6–§12: `GET /api/config` and the demo accounts panel; the open attempt includes a finished but unsubmitted run (PD-028); AnswerRequest validation before any lock; `feedback_source` on the Report; six World card states plus `is_maya_pick`; Progress fields; the coach runs after the grading commit (two-phase submit); `stimulus.rate`; content-integrity rules; the catalog format; demo first names; `docs/ai/AI_ARCHITECTURE.md` added to the ai-coach-engineer's outputs.
- Verification: document review only (no code existed yet).
- Human review: the human decided every change listed above. _Human: add anything else you changed by hand._

### 3. 2026-09-24 · `tech-lead` — G0: application skeleton and frozen contracts

- Files:
  - Backend: `backend/pyproject.toml`, `backend/uv.lock`, `backend/app/main.py`, `backend/app/core/{config,errors}.py`, `backend/app/api/error_handlers.py`, `backend/app/api/routers/health.py`, empty packages for `engine, models, repositories, services, ai, schemas, api/routers`, `backend/scripts/validate_content.py`, `backend/tests/{conftest.py,test_health.py,contracts/test_content_contracts.py}`, placeholder folders (`alembic/`, `seed/`, `tests/{engine,api,services,ai}/`).
  - Frontend: Vite + React 19 + TypeScript 5.9 + Tailwind v4 app in `frontend/` (ESLint + Prettier configs, Vitest setup, feature folders, `src/styles/tokens.css` placeholder, one smoke test).
  - Contracts: `docs/contracts/{items,mission,catalog}.schema.json`, `api-contract.md`, `conventions.md`, `CHANGE_REQUESTS.md` (G0 freeze notes F-01…F-14 and conflicts C-01…C-07); this log.
- What the agent did: scaffolded both apps as in SHARED_CONTEXT §11, froze the three content schemas and the API contract (every §8 endpoint, StateView, Report, World card, Progress, error model, idempotency, forbidden fields), and wrote the conventions.
- Verification (run by the agent): backend `uv sync`, `uv run ruff check .` and `ruff format --check .` clean, `uv run pytest` 32 passed (health, error envelope, schema positive/negative cases, catalog valid); `uvicorn app.main:app` booted and `GET /api/health` returned `{"status":"ok"}`, an unknown route returned the 404 envelope, and a 5-character `JWT_SECRET` was refused at boot; `scripts/validate_content.py` passed on `content/`. Frontend `npm run lint`, `typecheck`, `test` (1 passed), `build` and `format:check` all clean. Not verified: anything needing PostgreSQL (docker-compose belongs to the delivery-engineer), Python 3.12 specifically (the machine has 3.13).
- Human review: pending (G0).

### 4. 2026-09-24 · `narrative-designer` — The Last Train, Step A (item-independent story)

- Files: `content/missions/the-last-train/SCRIPT.md` (Step A draft), `docs/design/BACKDROPS.md` (created). `mission.json` **not** written: `items.json` did not exist yet when Step A finished, so Step B (checkpoints, consequences, per-checkpoint Maya lines, variants, rescue edges) is pending.
- What the agent did: wrote the premise (intro node, under 80 words), the location/backdrop sequence of the night, three flags (`wrong_ticket`, `wrong_platform`, `befriended_guard`) and what they change, the minutes model (correct 1 / incorrect 3 / rescue 1 on checkpoints 8–10; all-correct = 10 story-minutes; `train_departed` first reachable after checkpoint 7), the night-bus Plan B, the three endings with titles and closing lines, and Maya's generic mood lines (3 per mood). Proposed 11 backdrop keys; `docs/design/UI_SPEC.md` did not exist, so the keys must be reconciled with the ux-ui-designer.
- Verification: not run (no `mission.json` yet; the engine validator CLI does not exist yet). Minutes arithmetic checked by hand in SCRIPT.md §4.
- Human review: pending.

### 4. 2026-09-24 · `delivery-engineer` — phase 0: local infrastructure

- Files: created `.gitignore`, `docker-compose.yml`, `docker/db-init/01-create-test-db.sql`, `.env.example`, `Makefile`; appended CR-001 to `docs/contracts/CHANGE_REQUESTS.md`; created a local, git-ignored `.env` from `.env.example`.
- What the agent did: PostgreSQL 16 compose service with a `pg_isready` healthcheck, published on host port **5433** (the dev machine already runs a native PostgreSQL on 5432) and a one-time init script that creates `gam_test` for pytest; `.env.example` with every §10 + F-07 variable and working local defaults (psycopg 3 URL, 55-char dev-only `JWT_SECRET`, `COOKIE_SECURE=false`, `COACH_PROVIDER=mock`, `DEMO_MODE=true`, Anthropic vars commented out because an empty value is not "unset" for pydantic-settings); a POSIX-sh Makefile (`help env install up down reset migrate seed test lint gen-api validate-content`, with `migrate`/`seed` printing "not implemented yet" until `backend/alembic.ini` / `backend/seed/__main__.py` exist). Hosting chosen for the post-G2 deploy: **Render** — one Docker web service built from the repo's Dockerfile plus Render managed PostgreSQL 16 (same image as local, HTTPS out of the box, no infra code; caveats: free web instances sleep, the free database expires, Render's `postgresql://` URL must be rewritten to `postgresql+psycopg://`, and `COOKIE_SECURE=true` there).
- Verification: `docker compose config` valid (db on 5433, healthcheck, init mount). Recipes run by hand because GNU make is not installed on the machine: backend `uv run pytest` 32 passed, `ruff check` and `ruff format --check` clean, `scripts/validate_content.py` "content is valid"; OpenAPI export one-liner produced a valid 3.1 document (written to a scratch path, not to `docs/contracts/`) and `openapi-typescript` generated types from it; frontend `npm test` 1 passed, `lint`, `typecheck`, `format:check` clean. `git status --short --ignored` shows `.env`, `backend/.venv/`, `frontend/node_modules/` and caches ignored, `.env.example` tracked. **Not verified:** the container itself — Docker Desktop 4.45.0 crashed on start (`initializing Inference manager … remove …\AppData\Local\Docker\run\dockerInference: The file cannot be accessed by the system`), so `docker compose up -d --wait db` was never run.
- Human review: pending.

### 6. 2026-09-24 · `story-graph-engineer` — fixture mission and story graph engine

- Files: `content/missions/_fixture/{items,mission}.json` (replaces `.gitkeep`); `backend/app/engine/{__init__,graph,state,transition,heuristic,maya,validator,simulator,diary,cli}.py`, `backend/app/engine/README.md`; `backend/tests/engine/{conftest,helpers,test_transition,test_heuristic_and_maya,test_validator,test_simulator_and_diary}.py` (replaces `.gitkeep`); `docs/contracts/CHANGE_REQUESTS.md` (CR-001, CR-002).
- What the agent did: wrote the fixture (10 checkpoints in the §4 order and distribution, braided ok/fail consequences, rescue edges from checkpoint 7, flags `lost_ticket` and `wrong_platform`, Plan-B variants, three endings; 38 nodes, 55 edges, generated once by a throwaway script that is not committed) and the pure-Python engine: MissionGraph, State, ACTIONS/RESULT/goal test with grading and normalization, h by Dijkstra on the reversed graph, Maya's policy (hint at slack < 3, rescue with a one-step lookahead, see CR-001), mood FSM and line selection, the BFS/Kahn/DFS validator with content integrity, the seeded simulated student and calibration, the Diary from parent pointers, the clock label, a CLI and 70 engine tests.
- Verification (run by the agent, in `backend/`): `uv run python scripts/validate_content.py` → content is valid (fixture included; the script already covered every mission folder, so it was not changed); `uv run python -m app.engine.cli validate` → `_fixture: ok`, 1,024 paths, 10–30 story-minutes, endings by path made_it 386 / made_it_with_maya 246 / night_bus 392; `uv run pytest` → 102 passed, 1 skipped (the real-mission validator test, skipped until `the-last-train/mission.json` exists); `uv run ruff check .` and `ruff format --check .` clean. Calibration over 1,000 runs per profile is in the engine README. Not verified: the real mission (not written yet).
- Human review: pending. Known deviation: the engine is about 850 physical lines after `ruff format`, above the brief's "aim for under 400"; the validator (content integrity plus the path DFS) accounts for about 200 of them.

### 7. 2026-09-24 · `assessment-designer` — The Last Train items and assessment spec

- Files: created `content/missions/the-last-train/items.json` and `docs/assessment/ASSESSMENT_SPEC.md`.
- What the agent did: wrote the 10 original items in the §4 blueprint. The levels are A1 q01–q02, A2 q03–q05, B1 q06–q08 and B2 q09–q10. There are 4 multiple choice (grammar q01 and q09, listening q02 and q07), 2 fill_blank (grammar q05 and q06), 2 comprehension (reading q03 and q10) and 2 vocabulary (q04 and q08). Each item has a stimulus, diagnostic distractors, an explanation and a strategy hint. The spec contains: the blueprint with language points; the grading and integer round-half-up scoring; the level rule, with the evidence cap and the exact level-reason sentence template; 9 worked examples, including an A1 slip and a lucky B2 guess; the fill-blank normalization, written to match the engine's existing `normalize_answer`; the deterministic strength/challenge tie-break rule; the interpretation templates (can-do statement per level, strong/developing/focus lines per skill, and a recommendation per challenge skill); the item-review checklist (10/10 pass); and the story facts the items fix, for the narrative-designer.
- Verification: `cd backend && uv run python scripts/validate_content.py` gave "content is valid" (the-last-train `items.json` ok; `mission.json` skipped, not written yet). `uv run python -m app.engine.cli validate --mission the-last-train` stopped with `FileNotFoundError` for `mission.json`, because the engine validator needs the mission graph. A scratch self-check script confirmed: the blueprint counts and CEFR order; unique option ids and texts; every key among its options; each fill_blank prompt with exactly one `___`; the accepted answers already normalized (`normalize_answer(a) == a`) and de-duplicated; the audio word counts (24 and 34) and rates; the reading word counts (45 and 82); and all 9 worked examples, including their reason sentences and strength/challenge values.
- Human review: pending.

### 4. 2026-09-24 · `ux-ui-designer` — visual language, tokens, Maya avatar, screen specs

- Files: created `docs/design/UI_SPEC.md`, `frontend/src/styles/backdrops.css`, `frontend/src/assets/maya/{curious,encouraging,worried,proud}.svg`, `frontend/src/assets/backdrops/{arches,platform,skyline,bus,carriage}.svg`; replaced the placeholder `frontend/src/styles/tokens.css` (Tailwind v4 `@theme static`, default palette reset so no green/red utilities exist).
- What the agent did: defined "London after dark" (ink/navy, sodium-amber departure board, Maya's warm light; Atkinson Hyperlegible, Fraunces, Share Tech Mono), tokens with numerically checked WCAG contrast (lowest text pair fog-400/ink-700 6.22:1), component inventory, mobile-first specs with all states for every PRODUCT §5 screen, one renderer per item type and stimulus kind (incl. PD-013/PD-014 listening), motion (Must/Should) and keyboard/ARIA rules, microcopy, a proposed backdrop-key list for the narrative-designer, and an original four-mood SVG avatar for Maya. SVGs generated by small Node scripts and checked visually with headless Chrome.
- Verification: in `frontend/`: `npm run build` OK (CSS 26.6 kB, 6.2 kB gzip), `npm run lint` clean, `npm run format:check` clean, `npm test` 1 passed. Contrast computed with the WCAG relative-luminance formula (table in UI_SPEC §3.2). No React components written.
- Human review: pending.

### 8. 2026-09-24 · `narrative-designer` — The Last Train, Step B (story graph around the 10 items)

- Files: `content/missions/the-last-train/mission.json` (37 nodes, 53 edges), `content/missions/the-last-train/SCRIPT.md` (rewritten as the full screenplay: premise, the night's route, flags, minutes, Plan B, endings, validation and calibration, every node with both branches, mood lines, leak check), `docs/design/BACKDROPS.md` (reconciled with the 8 keys in UI_SPEC §6). The node texts were written by the agent and turned into JSON by a throwaway helper script in the agent's scratchpad (not in the repository).
- What the agent did: put the 10 items (used verbatim) into a braided graph. Intro, then per checkpoint an `_ok` and a `_fail` consequence that both lead to the next checkpoint, rescue scenes at checkpoints 7, 8 and 10, and three endings. Three flags (`wrong_platform` ← c02 incorrect, `wrong_ticket` ← c04 incorrect, `befriended_guard` ← c05/c08 correct) with later variants. `train_departed` night-bus-to-Oxford variants on the 11 nodes the validator lists. Maya's intro, reaction_ok and reaction_fail on every checkpoint (plus reaction_rescue on 7, 8 and 10), a closing line per ending, 3 mood lines per mood, and a Diary line on every node. Followed ASSESSMENT_SPEC §9: story facts kept, no answer given away before its checkpoint.
- Verification: `uv run python scripts/validate_content.py` → content is valid. `uv run python -m app.engine.cli validate --mission the-last-train` → ok, 1024 paths, 10–30 story-minutes, endings by path made_it 386 / made_it_with_maya 176 / night_bus 462, 11 nodes reachable with train_departed. `calibrate --profile all` (1,000 runs each), made the train: A1 25.0%, A2 79.6%, B1 98.9%, B2 100%, A2_weak_listening 65.5%. All §5 targets met, so the default edge minutes were kept. All-correct path = 10 story-minutes. No change requests were needed.
- Human review: pending (G1).

### 9. 2026-09-25 · `data-engineer` — schema, migration, repositories, seed with demo users and veteran history

- Files: `backend/app/models/{base,reference,identity,content,attempts,coach,__init__}.py`, `backend/alembic.ini`, `backend/alembic/{env.py,script.py.mako}`, `backend/alembic/versions/0001_initial.py` (hand-written), `backend/app/repositories/{__init__,db,users,missions,attempts,progress,coach}.py` (signature index in `__init__.py`), `backend/app/core/demo.py`, `backend/seed/{__init__,__main__,run,content,users,veteran}.py`, `backend/tests/data/{conftest,test_schema,test_repositories,test_seed}.py`, `docs/data/DATA_MODEL.md`, CR-005 and CR-006 in `docs/contracts/CHANGE_REQUESTS.md`.
- What the agent did: 16 tables (no graph tables: the story graph is versioned JSONB in `mission_versions.content`; no stored profile). Invariants in the database: at most one correct option (partial unique), one open attempt per (student, mission) (partial unique on in_progress/completed), one answer per checkpoint (UNIQUE) with exactly one of option/text (CHECK), root/child steps (CHECK). Answer keys only through `get_answer_key` / the server-only `load_mission_content`. Idempotent seed: schema + engine validation, content-hash versioning, demo users (argon2), class, and the veteran's 4 attempts from the simulator replayed through the engine's real RESULT and scored with the backend's `grading`/`leveling` and the deterministic coach. Veteran seeds (6, 9, 1, 4) were chosen on the real content for a 40→50→60→70% trend with listening as the challenge. Diagnosed the local hang: `localhost` → IPv6 `::1` stalls without a connect timeout (CR-005); added `connect_timeout` to the engine and Alembic.
- Verification (run by the agent, compose PostgreSQL 16 on 5433): `alembic upgrade head` twice (second a no-op, `alembic current` = 0001 head); `python -m seed` twice on `gam` (first created version 1 of the real The Last Train content, 3 users, 4 veteran attempts with 88 steps and 40 answers; second changed nothing); `alembic check` (models = migration) inside the tests; `uv run ruff check .` and `ruff format --check .` clean; `uv run pytest` 190 passed (including 18 data tests against `gam_test`). The veteran history was deleted by hand once in `gam` to re-seed it with the new seeds.
- Human review: pending (G1).

### 10. 2026-09-25 · `backend-api-engineer` — API walking skeleton: auth, world, attempt lifecycle, report, progress, teacher

- Files: `backend/app/core/{security,rate_limit,logging}.py` (new), `backend/app/core/config.py` (optional `SPA_DIST_DIR`); `backend/app/api/{deps,middleware,spa}.py` (new), `backend/app/api/routers/{auth,attempts,world}.py` (new); `backend/app/main.py`; `backend/app/schemas/{common,auth,state,attempts,report,world}.py`; `backend/app/services/{auth,attempts,content,state_view,grading,leveling,coach,feedback,report,world}.py`; `backend/scripts/export_openapi.py` → `docs/contracts/openapi.json`; tests `backend/tests/{fakes,leak}.py`, `backend/tests/api/{conftest,helpers,test_auth,test_mission_flow,test_attempt_security,test_postgres_flow}.py`, `backend/tests/services/{test_grading_leveling,test_state_view,test_coach_and_limits}.py`; `docs/contracts/CHANGE_REQUESTS.md` (CR-004).
- What the agent did: every endpoint of api-contract §5.1–§5.15 (not the stretch `/simulate`), against the data-engineer's published repositories and the engine's public API. Login with argon2 (same error for unknown email/wrong password, dummy verify for timing), JWT in the httpOnly SameSite=Lax `gam_session` cookie, in-memory 5/min rate limit per (IP, email) with 429 + Retry-After, `current_user` reloads the user every request, `require_role`. Request-id middleware, JSON logs, `Cache-Control: no-store`, the error envelope for everything including unhandled errors. `to_state_view` is the only mapper to the client and copies fields from a whitelist. Answer follows the contract's fixed order (shape → 404 → 409 locked → 409 closed → 409 out of sequence → 422 item rules → row lock + re-check → grade/RESULT/persist); every 409 carries `details.state`. Submit is two-phase (grade + level under `FOR UPDATE`, commit; then the coach outside any transaction with an 8 s budget, feedback `ON CONFLICT DO NOTHING`, memory updated only when the row was written). grading/leveling implement ASSESSMENT_SPEC §3–§5 and §7.1 exactly; the fallback feedback uses its §7 templates; normalization comes from `app.engine.normalize_answer` (CR-003). Mission graphs are cached per immutable version id. FastAPI serves `frontend/dist` when it exists (single origin).
- Verification (run by the agent in `backend/`): `uv run ruff check .` and `ruff format --check .` clean; `uv run pytest` → 190 passed (69 of them new: API flow over an in-memory fake of the repositories, including the fixture and the real mission end to end, the recursive forbidden-field scan over every in-progress response, all §9 invariants, the 9 ASSESSMENT_SPEC worked examples, `to_state_view` over every node × flag set × decision of both missions, coach timeout/invalid-output fallback, rate limiter). **Against real PostgreSQL 16** (compose, `gam_test` on 127.0.0.1:5433): `tests/api/test_postgres_flow.py` migrates, seeds and plays login → start → full mission → submit twice → progress over the real repositories. Also a read-only smoke test of `uvicorn` against the seeded `gam` database as Leo (world, progress, a seeded report, teacher classes, 404 on a foreign attempt, `/api/docs` 200). Not done: a mutating run on the `gam` dev database (kept clean for the demo users), a browser run through Swagger UI.
- Human review: pending (G1). Review first: `services/state_view.py`, `services/grading.py`, `services/leveling.py`, `services/attempts.py` (answer order and two-phase submit).

### 11. 2026-09-25 · `ai-coach-engineer` — Maya's narrator layer (coach port, Claude adapter, deterministic mock, memory)

- Files: `backend/app/ai/{__init__,ports,templates,prompting,mock_provider,anthropic_provider,factory,service,samples}.py`, `backend/app/ai/prompts/maya_feedback_v1.md`, `backend/tests/ai/{conftest,helpers,test_mock_and_ports,test_service_and_fallback,test_factory_and_seam}.py`, `docs/ai/AI_ARCHITECTURE.md` (Spanish), `docs/ai/samples/{new_student,veteran}.json`; seam edit in `backend/app/services/coach.py` (CR-004: templates re-exported from `app.ai.templates`, `deterministic_feedback` delegates to the AI mock with the old assembly as a safety net); CR-007 in `docs/contracts/CHANGE_REQUESTS.md`.
- What the agent did: a `CoachProvider` port with strict Pydantic models (skill codes, echo of the deterministic strength/challenge, next mission among the candidates, per-field length limits, MAYA.md banned words and emojis); a Claude adapter on the official SDK (structured output via `output_config.format` JSON schema with enums, `effort: low`, `max_tokens` 1500, 7 s HTTP timeout, no retries, refusal/truncation/malformed JSON → error); a deterministic mock from ASSESSMENT_SPEC §7 plus coach memory (first meeting vs returning student, never invents a memory); `factory.get_coach_provider()` for submit (missing key, unknown value or constructor error → mock); `service.generate_feedback` with the 8 s budget and fallback, `update_memory` (sessions +1, last 5 notes, next greeting); prompt-injection defences (sanitized first name, user text only as escaped JSON inside `<mission_result>`); the Spanish architecture page with cost and latency estimates. Default `ANTHROPIC_MODEL` suggestion: `claude-opus-5`.
- Verification (run by the agent in `backend/`): `uv run ruff check .` and `uv run ruff format --check .` clean; `uv run pytest` → 234 passed (44 new in `tests/ai`, plus the existing Postgres end-to-end submit flow, which now stores the AI mock's feedback). No real Claude call was made: `ANTHROPIC_API_KEY` is not configured, so the samples contain the mock only; the Claude adapter is covered with a fake SDK client (valid output, invalid skill, unknown mission, malformed JSON, refusal, timeout and API errors).
- Human review: pending.

### 12. 2026-09-25 · `frontend-engineer` — G1 walking skeleton in the browser: Check-in, English World, Mission Player, Ending, Mission Report, Progress

- Files: `frontend/src/{app,api,components,lib,routes,features/{auth,world,mission,report,progress,teacher}}/**`, `frontend/src/api/schema.d.ts` (regenerated by `npm run gen:api` from a fresh `backend/scripts/export_openapi.py` export), `frontend/src/api/mock/*` (dev-only mock adapter, `npm run dev:mock`, `.env.mock`), `frontend/index.html` (Google Fonts + theme-color), `frontend/package.json` (`lucide-react`, `dev:mock` script), `frontend/vite.config.ts` (proxy target 127.0.0.1, CR-005), `frontend/src/test/setup.ts`; removed the placeholder `App.tsx`/`App.test.tsx`; CR-008 in `docs/contracts/CHANGE_REQUESTS.md`.
- What the agent did: typed fetch wrapper (cookie only, error envelope → `ApiError`, global 401 → session-ended flow); one `ApiClient` interface with the real client and a mock loaded only when `import.meta.env.DEV && VITE_API_MOCK=true`; route guard on `/api/auth/me` with role landing (teacher → PD-019 page); Check-in boarding pass with validation, the three error copies and the demo panel from `/api/config`; English World (greeting, six card states, Maya's pick, snapshot, skeleton/error); Mission Player as a reducer state machine (`features/mission/playerReducer.ts`) that renders only the StateView, runs each accepted request exactly once (double-submit guard in the reducer and in the checkpoint), resyncs silently on every 409, keeps the failed request for Try again, re-reads the state on reconnection; renderers per item type and per stimulus kind (announcement with speechSynthesis and the PD-014 fallback, phone message, notice, timetable, sign, dialogue); keyboard 1–4 / Enter / L; one polite live region; Ending with Submit mission and the "Maya is reading your Diary…" state; Mission Report in the PD-001 order with the provider label from the server; Progress with the Attempt history table, profile, Maya's notes, open attempt and empty state.
- Verification (run by the agent): `npm run lint`, `typecheck`, `format:check`, `test` (4 files, 23 tests: player reducer, FillBlank, listening fallback, Check-in) and `build` all pass; the production bundle contains no mock code, no demo password and no answer-key field names. Full browser runs with headless system Chrome (playwright-core, scratch script outside the repo) against uvicorn + the seeded `gam` database through the Vite proxy: Ana (`new@`) played The Last Train end to end four times (1280 px and 360 px), including a reload mid-mission (same node, same clock), a reload on the Ending, Submit → Report → Progress → World → Check out; a recursive scan of every open-attempt response found no forbidden keys; no horizontal overflow at 360 px. Leo (`veteran@`): World with memory greeting and Maya's pick, Progress with 4 rows, a past report (read-only, no attempts created). Teacher landing, student redirect from `/teacher`, 404 report/run, unknown route and the session-ended redirect were checked too. Found and fixed during the run: `AnimatePresence mode="wait"` could leave the previous node (with a disabled Continue) on screen; the content now only fades in, keyed by node.
- Side effect: Ana now has 4 submitted attempts in `gam`; reset before the demo (`make reset`, i.e. `docker compose down -v` + migrate + seed).
- Human review: pending (G1). Review first: `features/mission/playerReducer.ts`, `features/mission/MissionPlayerPage.tsx`, `api/http.ts`, `api/client.ts`.

### 13. 2026-09-25 · `qa-engineer` — test plan, gap tests, Playwright E2E, live checks, demo script

- Files: created `docs/qa/{TEST_PLAN,CHECKLIST,DEMO_SCRIPT}.md`, `backend/tests/api/{test_qa_gaps,test_postgres_concurrency}.py`, `backend/tests/engine/test_real_mission_invariants.py`, `backend/tests/contracts/test_spa_has_no_answers.py`, `frontend/playwright.config.ts`, `frontend/tsconfig.e2e.json`, `frontend/e2e/{helpers.ts,mission.spec.ts,resilience.spec.ts,serve.sh,reset_e2e_db.py}`; changed `frontend/{package.json,package-lock.json}` (`@playwright/test` 1.56.1 pinned, `test:e2e` script), `frontend/{tsconfig.json,eslint.config.js,.prettierignore}` (e2e project, ignore Playwright outputs), `Makefile` (`make e2e`, `make test E2E=1`). No production code changed.
- What the agent did: risk-based plan and traceability matrix (every cited test name checked with a script); added 17 backend tests (401 code on every endpoint, leak scan of 409 bodies, resume from a new session, AI provider failures through submit with identical grades, concurrent answers and submits on PostgreSQL, veteran progress over HTTP, the 1,024 paths of the real mission checked independently of the validator, SPA source/bundle answer scan); two Playwright specs over the real stack on a throwaway `gam_e2e` database (keyboard-only journey at 1280 and 360 px, Maya's rescue, two tabs, reload, no speech, reduced motion); six live checks in headless Chromium, including the report footer with `COACH_PROVIDER=anthropic` and no/invalid key against a local fake Messages API (no real Anthropic call, no real key); the Spanish demo script with a dry-run answer sequence. Found BUG-001 (S2, Progress table overflows at 360/768 px) and BUG-002…005 (S3), with proposed fixes.
- Verification (run by the agent): `uv run pytest` → 253 passed in ~26 s; `ruff check` / `ruff format --check` clean; `npm test` → 26 passed; `npm run lint`, `typecheck`, `format:check` clean; `make e2e` → 6 passed (one of them the expected failure for BUG-001), 1 skipped; the full E2E suite passed 4 runs in a row after hardening the screen detection (two earlier runs had failed on a timing race in the helper, fixed). The demo database `gam` was not written.
- Human review: pending.
