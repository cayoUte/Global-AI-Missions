# Product DECISIONS LOG — Global AI Missions

> Owner: `product-architect`. Running log of product decisions. Newest at the bottom.
> Status: **Decided** (in force) · **Proposed** (needs the human or the Tech Lead to confirm) · **Superseded**.
> Anything that touches contracts is also raised in `docs/contracts/CHANGE_REQUESTS.md` by the affected owner once contracts are frozen (G0).

| ID | Date | Decision | Reason | Affects | Status |
|---|---|---|---|---|---|
| PD-001 | 2026-09-24 | The Mission Report opens with the result, in the brief's format and without clicks: label `A2 · The Last Train — 70%`, global %, per-skill %, correct/incorrect, suggested level with its reason, attempt record. Then Maya's interpretation, then the next-mission card, then the Diary, then missed checkpoints. SHARED_CONTEXT §7 and the affected agent briefs were updated to match. | The brief's §5 lists exactly these as what the result screen must show and gives the headline format ("A2 English Assessment – 82%"); evaluators look for it first. "Story first" still holds: numbers never appear during the mission. Maya right after reads the numbers the student has just seen. | frontend, ux-ui | Decided (human delegated, 2026-09-24) |
| PD-002 | 2026-09-24 | Diegetic labels everywhere **except** where they would hide a required function: the verbs **Submit mission** and **Progress**, the report's labels (Global score, Result per skill, Correct, Incorrect, Suggested level, Attempt record) and **Attempt history** on Progress stay plain. | Evaluators tick a checklist; they must find "submit", "progress" and the numbers without translation. | frontend, ux-ui, narrative | Decided |
| PD-003 | 2026-09-24 | Submission is an explicit step on the Ending screen (**Submit mission**), after the story ends. Per-checkpoint answers are locked as they are given; submit grades, runs the coach and closes the attempt. | Makes the brief's "submit the answers" visible while keeping the story uninterrupted. | backend, frontend | Decided |
| PD-004 | 2026-09-24 | The Ending screen shows no score, level or count. | The ending is story; numbers belong to the report (story first, numbers after). The ending never changes the score. | frontend, narrative | Decided |
| PD-005 | 2026-09-24 | Choice checkpoints use a two-step decision: select, then **Confirm**. | Answers lock on first submission (§9.3); a misclick must not cost a checkpoint. | frontend, ux-ui | Decided |
| PD-006 | 2026-09-24 | English World card states (now in SHARED_CONTEXT §7 and §8): Available, In progress, Waiting to submit, Completed, Locked, In preparation (+ at most one "Maya's pick" marker). | Covers every attempt status (`in_progress`, `completed`, `submitted`) and the locked catalog honestly. | backend (`/api/world`), frontend, ux-ui | Decided (2026-09-24); Tech Lead freezes it in the contract at G0 |
| PD-007 | 2026-09-24 | The four non-playable missions are never playable in the MVP. When their unlock rule is met they show "Maya is preparing this mission." instead of a playable state. | Honest about scope; still demonstrates deterministic unlock rules and the "simulated next mission" (§3). | backend, frontend | Decided |
| PD-008 | 2026-09-24 | Unlock rules are deterministic data in `content/catalog.json` with kinds `always`, `mission_submitted {mission_id}`, `level_reached {min_level}` (level from any submitted Mission Report) and a human-readable `hint`. The LLM's recommendation never unlocks anything. | §9.6: the LLM never influences unlocks; rules must be explainable. | data-engineer (JSONB), backend, tech-lead (catalog schema) | Decided (human, 2026-09-24); Tech Lead freezes the catalog schema |
| PD-009 | 2026-09-24 | Profile (rolling per-skill performance) = per-skill % over the last 3 submitted attempts (all missions), computed by query (not stored), shown with the number of attempts it is based on. | Simple, explainable, shows a trend for the veteran without one bad night dominating. | backend, frontend | Decided (human, 2026-09-24) |
| PD-010 | 2026-09-24 | Next-mission candidates = the four non-playable catalog missions. Deterministic default (used by the mock/fallback) maps the challenge skill to the mission training it: grammar → the-interview, vocabulary → dinner-for-two, reading → campus-day, listening → night-radio. | Each locked mission trains exactly one scored skill, so the recommendation is always explainable and the fallback is trivial. | ai-coach-engineer, backend | Decided |
| PD-011 | 2026-09-24 | There is **no** skip action on checkpoints. Every checkpoint requires a decision (select/type, then Confirm) to continue, so every item is always answered. | Rejected by the human: a skip button makes no sense in the story and must not become an incorrect answer. Supersedes the earlier "I'm not sure — keep going" proposal; no contract change needed. | tech-lead (contract), backend, frontend | Decided (human, 2026-09-24) |
| PD-012 | 2026-09-24 | Hints come only from Maya's planner (slack < 3). There is no student-initiated "Ask Maya / Hint" button. | Keeps the planner the single authority over Maya's actions; avoids a free-chat surface. | frontend, story-graph | Decided |
| PD-013 | 2026-09-24 | Listening stimuli (speech rate from `stimulus.rate`): the student presses **Listen**; **Listen again** is unlimited; no transcript before answering; the transcript appears in "Checkpoints to revisit" after submission. | Browsers block autoplay; unlimited replays are fair for a prototype and simpler to explain than counting plays. | frontend, ux-ui, narrative | Decided |
| PD-014 | 2026-09-24 | If speech synthesis is unavailable, the Listen area explains it and shows the announcement as text so the story continues. Documented as a limitation. | The mission must never block; the listening script already reaches the browser (§9 known trade-off). | frontend, tech-lead (DECISIONS.md) | Decided (human, 2026-09-24); limitation goes in DECISIONS.md |
| PD-015 | 2026-09-24 | The suggested level on the report always carries a one-line explanation of the rule applied (base band + evidence cap). | "Deterministic and explainable" should be visible to the student and the evaluator, not only in code. | backend (report DTO), assessment-designer (templates), frontend | Decided |
| PD-016 | 2026-09-24 | The report shows a footnote disclosing the feedback source from the Report's `feedback_source`: LLM ("Written by Maya using {provider_label}", e.g. Claude) or mock/fallback ("from her notebook (offline feedback)"). The UI never hard-codes a provider name. | Honesty about AI; lets the evaluator see both paths; never presented as an error. | frontend, ai-coach-engineer | Decided |
| PD-017 | 2026-09-24 | Speaking is shown on the report only as a muted note: "Speaking isn't measured in this mission yet." | The brief lists Speaking; we are honest that it is roadmap and never show a fake 0%. | frontend | Decided |
| PD-018 | 2026-09-24 | In demo mode, Check-in shows a "Demo accounts" panel with the three seeded users and the shared demo password, read from the public `GET /api/config`; hidden otherwise. No credentials in the frontend bundle. | Evaluators must reach every journey in seconds. The password is demo-only. | frontend, backend (expose demo mode flag) | Decided (human, 2026-09-24) |
| PD-019 | 2026-09-24 | Teacher/admin check-in: if the Teacher view stretch is not built, they land on a plain read-only page stating the view is not part of this build; RBAC is then demonstrated through the API. | Keeps the teacher demo user meaningful without committing to the stretch. | frontend | Decided |
| PD-020 | 2026-09-24 | Completed missions offer **Play again**; each run is a new attempt with its own report. | Needed for the veteran's history and trend; retake is practice, not secure re-assessment (§9 trade-off). | backend, frontend | Decided |
| PD-021 | 2026-09-24 | Maya's relationship stage is derived from `sessions_count`: 1 = first meeting, 2–3 = getting to know you, 4+ = familiar companion. It changes the World greeting and the report voice; in-mission lines are the same for everyone in the MVP. | Delivers "Maya remembers you" where it is cheap (greeting, report) without multiplying authored content. | ai-coach-engineer, narrative | Decided |
| PD-022 | 2026-09-24 | Canonical first-meeting greeting (no memory): "Hi, I'm Maya. Tonight we have one job: get you on the last train home. Ready when you are." | Deterministic line for the empty-memory state of the English World. | backend (world), ai-coach-engineer | Decided |
| PD-023 | 2026-09-24 | Demo first names: `new@globalai.test` = Ana, `veteran@globalai.test` = Leo; teacher = Ms. Clarke. | Maya greets by first name; the seed needs names. | data-engineer | Decided (human, 2026-09-24) |
| PD-024 | 2026-09-24 | The Mission Player must be completable by a practised presenter in ≤ 5 minutes: narrative nodes ≤ 3 short lines, no forced waits. | The whole evaluator demo is 10 minutes. | narrative, frontend | Decided |
| PD-025 | 2026-09-24 | `skill_focus` in the catalog is an array of scored skills (The Last Train lists four; the others one each); `cefr_range` is `{min, max}`. | The Last Train is multi-skill; `{min, max}` maps directly to `cefr_min`/`cefr_max` columns. | data-engineer, tech-lead | Decided (human, 2026-09-24); Tech Lead freezes the catalog schema |
| PD-026 | 2026-09-24 | Stretch order: (1) Teacher view read-only, (2) Simulated replay (demo mode only). Nothing else is a stretch goal. | Both serve evaluator criteria (roles, AI as search); anything else is out of scope. | all | Decided |
| PD-027 | 2026-09-24 | Budget is 8 hours (brief §10: one day, 6–8 h, no overnight work). The plan adds a walking-skeleton gate at 2:30 that runs the whole §1 flow on a fixture mission; polish (typewriter, clock flip, Maya idle, waveform, chart) becomes Should after G2; `/simulate` and the replay are stretch; calibration is one informational run. | The brief judges reasoning and execution in one day, not completeness; the skeleton is the fallback deliverable. | all | Decided (human, 2026-09-24) |
| PD-028 | 2026-09-24 | A finished run that has not been submitted stays the student's open attempt: start returns it, the World shows "Waiting to submit" and a reload on the Ending shows the same Ending with **Submit mission**. | Otherwise a reload or an expired session before Submit would lose the run and its attempt record. | tech-lead, data-engineer, backend, frontend | Decided |
| PD-029 | 2026-09-24 | After submission, the Diary and the missed checkpoints tag each checkpoint with type · skill · level (e.g. "Multiple choice · Grammar · A2"); the README shows the 10-item blueprint. Never during the mission. | Lets the evaluator verify the 4/2/2/2 types and per-item levels (brief §4) without opening JSON; the mission stays immersive. | backend (report), frontend, delivery | Decided |
| PD-030 | 2026-09-24 | The Teacher view (stretch) has no drill-in to a student's individual report; a teacher asking for another class gets not-found; admin sees all classes. | Avoids weakening invariant 4 (foreign attempts → 404) for a stretch screen. | backend, frontend, qa | Decided |
| PD-031 | 2026-09-24 | The story clock reads "22:05 · Departing now" at 0 minutes (contract F-06). The answer response carries no outcome field (contract F-01). | Accepted by the human at G0. | backend, frontend | Decided (human, 2026-09-24) |
| PD-032 | 2026-09-25 | UI defaults from the ux-ui-designer accepted: (1) report portrait mood derived from the score (proud ≥ 70%, otherwise encouraging), no new Report field; (2) story-minutes shown as a single number ("16"), not "16 of 18"; (3) world_zone display name derived from the slug ("the-station" → "The Station"); (4) choices are enabled before Listen is pressed (no forced play, better accessibility); (5) Maya's reaction line may be lost on a reload during the consequence scene (acceptable for the MVP); (6) English World heading "Welcome, {name}."; (7) the Diary shows every step without collapsing (collapse only if time remains); (8) fonts from the Google Fonts CDN in the MVP; self-hosting noted for production in DECISIONS.md. | Keeps the contract frozen and the build within 8 hours; every item has a documented production alternative. | frontend, ux-ui, tech-lead (DECISIONS.md note on fonts) | Decided (human, 2026-09-25) |
